{
  "name": "darkorbit-flash-client-gateway",
  "version": "1.0.0",
  "private": true,
  "description": "Original Flash client and restricted WebSocket gateway for DarkOrbit",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "verify:ui": "node scripts/verify-web-ui.js",
    "test": "node --test test/*.test.js",
    "test:unit": "node --test test/*.test.js",
    "verify": "powershell.exe -NoProfile -ExecutionPolicy Bypass -File test/original-flash-client.test.ps1",
    "start:gateway": "node scripts/ws-proxy.js"
  },
  "dependencies": {
    "ws": "8.21.3"
  },
  "devDependencies": {
    "puppeteer-core": "^25.10.0"
  }
}
